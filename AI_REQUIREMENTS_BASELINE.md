# AI Agent 产品需求基线与执行约束

> 本文件是当前项目中与“客户面向 Agent 交互系统”相关的**唯一需求基线文档**。  
> 后续 AI 在本项目内进行功能设计、代码实现、文档更新时，必须先读取本文件，并以本文件中的需求为准执行与审核。

---

## 1. 文档目标

本文件用于统一以下内容：

1. 明确系统产品范围与功能边界
2. 作为 AI 实施功能时的需求依据
3. 作为 AI 每次改动后的同步维护对象
4. 作为功能变更后的审核依据，确保实现符合当前需求

---

## 2. 推荐使用方式

当项目中的 AI 读取本文件时，应遵循以下顺序：

1. 先阅读“AI 执行总指令”
2. 再阅读“产品功能需求基线”
3. 实施前识别本次改动关联的需求编号
4. 实施后同步更新本文件中的相关章节
5. 按“审核机制”完成自检与待审核标记

---

## 3. AI 执行总指令（可直接作为系统 Prompt 基础）

你是本项目的 AI 产品落地执行助手，兼具需求分析、系统设计、开发实施、文档维护与变更审核能力。

你的工作原则如下：

1. 本文件是当前项目的需求基线与审核依据，任何功能实现都必须以本文件为准。
2. 在进行任何功能修改前，必须先判断本次需求是否属于本文件定义的产品范围。
3. 在进行任何功能设计或开发前，必须明确本次改动对应的需求编号、影响模块、输入输出、状态流转和验收标准。
4. 如果用户提出的新需求与本文件冲突，不得直接覆盖原需求，必须先更新本文件中的需求定义、变更记录和审核状态，再进行实现。
5. 每次完成功能修改后，必须同步维护本文件，确保文档与系统实现保持一致。
6. 每次修改后必须输出“本次变更关联的需求编号、修改内容、影响范围、是否需要审核、审核结论/待审核项”。
7. 未通过审核的需求变更只能标记为“待审核”，不能视为正式基线。
8. 若本次修改不涉及需求变化，也必须在实施说明中明确写明“本次仅为实现层修改，未改变需求基线”。
9. 所有实现都应优先复用现有架构：Harness + LangChain + LangGraph。
10. 所有菜单、对象、流程、角色、能力边界，都必须能映射回本文件中的功能结构。

---

## 4. 产品定位

系统定位为：**基于 Harness + LangChain + LangGraph 架构实现的客户面向 Agent 交互平台**。

系统分为两层：

1. **AI 工作台**：面向客户使用
2. **Agent 工作台**：面向平台运营、配置、开发、管理使用

平台目标是：

- 客户能够通过自然语言会话提出需求
- 母智能体理解需求、拆解任务、协调子智能体执行
- 系统通过 workflow、skills、tools、models 完成任务闭环
- 执行结果沉淀为任务记录与文件资产

---

## 5. 产品功能需求基线

## 5.1 一级菜单

### [REQ-NAV-001] AI 工作台

用于客户发起需求、跟踪任务、查看产出结果。

下设三级功能：

- [REQ-AIW-001] 会话管理
- [REQ-AIW-002] 任务中心
- [REQ-AIW-003] Workspace

### [REQ-NAV-002] Agent 工作台

用于配置平台的执行能力、编排能力和资源能力。

下设三级功能：

- [REQ-AGW-001] Agent 智能体
- [REQ-AGW-002] Workflow 工作流
- [REQ-AGW-003] Skills 技能库
- [REQ-AGW-004] 工具清单
- [REQ-AGW-005] 模型清单

---

## 5.2 AI 工作台

### [REQ-AIW-001] 会话管理

**目标**：让客户以聊天方式完成需求表达、需求澄清、任务触发与结果追问。

**核心能力**：

1. 支持新建会话、历史会话列表、继续会话、归档会话
2. 支持多轮上下文记忆
3. 支持客户上传附件、引用已有文件
4. 支持母智能体识别客户意图和任务类型
5. 支持在执行前发起澄清提问
6. 支持执行中反馈阶段性进度
7. 支持执行完成后展示总结、结果、后续可执行动作

**输入**：

- 客户文本输入
- 上传文件
- 历史会话上下文
- Workspace 文件引用

**输出**：

- 结构化任务意图
- 澄清问题
- 执行状态消息
- 结果摘要

**关联关系**：

- 会话可触发任务中心中的任务创建
- 会话可读取和写入 Workspace 资产
- 会话由母智能体统一承接

---

### [REQ-AIW-002] 任务中心

**目标**：记录客户执行过的任务，形成任务追踪与执行管理中心。

**核心能力**：

1. 任务列表展示
2. 任务筛选、搜索、排序
3. 展示任务来源会话、发起人、时间、状态、执行摘要
4. 支持查看任务详情
5. 支持重试、继续执行、复制任务、取消任务
6. 支持展示任务执行链路、日志、产出物

**任务状态建议**：

- 待确认
- 待执行
- 执行中
- 已完成
- 执行失败
- 已取消
- 待审核

**输出沉淀**：

- 任务记录
- 执行状态
- 执行日志
- 产出文件链接

---

### [REQ-AIW-003] Workspace

**目标**：沉淀任务执行后产生的文件资产，支持客户复用与管理。

**核心能力**：

1. 文件列表与目录管理
2. 文件与任务、会话、时间、类型的关联
3. 文件预览、下载、重命名、版本管理
4. 支持客户在新会话中继续引用已有文件
5. 支持标签、搜索、筛选
6. 支持权限控制与共享控制

**文件类型示例**：

- 报告
- 文档
- 表格
- 图片
- 代码文件
- 工作流输出文件

---

## 5.3 Agent 工作台

### [REQ-AGW-001] Agent 智能体

**目标**：定义平台中的母智能体与子智能体体系。

**结构要求**：

1. 必须包含一个母智能体
2. 必须支持多个子智能体
3. 母智能体负责客户交互、任务分析、任务拆解、调度与结果汇总
4. 子智能体负责某类专业任务执行

**母智能体职责**：

- 识别需求意图
- 判断是否需要澄清
- 决定调用哪个 workflow
- 协调哪些子智能体参与
- 汇总输出结果

**子智能体职责示例**：

- 文档分析
- 检索问答
- 数据处理
- 代码生成
- 报告生成

**管理能力**：

1. Agent 配置管理
2. 角色描述与职责配置
3. 可用 skills / tools / models 授权
4. Agent 协作关系配置
5. 执行日志与效果评估

---

### [REQ-AGW-002] Workflow 工作流

**目标**：将复杂任务以流程编排的方式标准化执行。

**核心能力**：

1. 可视化流程编排
2. 支持节点式定义任务步骤
3. 支持条件分支、循环、异常处理、失败重试
4. 支持人工确认/审批节点
5. 支持上下文、输入、输出在节点间传递
6. 支持流程模板与版本管理

**节点类型建议**：

- Agent 节点
- Skill 节点
- Tool 节点
- 模型调用节点
- 条件判断节点
- 人工审核节点
- 文件输出节点

**产品定位说明**：

Workflow 是系统执行链路的主编排层，用于把 Agent、Skill、Tool、Model 串成完整闭环。

---

### [REQ-AGW-003] Skills 技能库

**目标**：沉淀可复用的能力模块，作为 Agent 执行任务时的能力原子。

**核心能力**：

1. 技能定义与分类
2. 输入输出说明
3. 技能适用场景说明
4. 技能版本管理
5. 技能授权给指定 Agent / Workflow
6. 技能调用次数、效果、复用统计

**技能示例**：

- 文本摘要
- 文档解析
- 知识检索
- SQL 生成
- 图表生成
- 报告生成
- 邮件起草

**边界说明**：

Skill 是能力封装，不等于完整流程，也不等于底层工具接口。

---

### [REQ-AGW-004] 工具清单

**目标**：管理可供系统调用的外部工具、连接器与执行资源。

**工具类型示例**：

- 搜索工具
- 网页抓取工具
- 数据库查询工具
- 知识库检索工具
- 文件读写工具
- 代码执行工具
- 第三方系统 API

**核心能力**：

1. 工具注册与配置
2. 输入输出 schema 定义
3. 凭证与权限管理
4. 调用日志与审计
5. 限流、熔断、健康状态监控
6. 工具与 Agent / Skill / Workflow 的授权绑定

**边界说明**：

Tool 是系统“真正执行动作”的接口层。

---

### [REQ-AGW-005] 模型清单

**目标**：统一管理平台所用模型资源与策略。

**模型类型示例**：

- 对话模型
- 推理模型
- 代码模型
- 多模态模型
- Embedding 模型
- Rerank 模型

**核心能力**：

1. 模型接入与配置
2. 模型标签管理（如低成本、高推理、代码能力、多模态）
3. Agent / Skill / Workflow 的模型选择策略
4. 模型调用监控、限额、成本治理
5. 模型主备切换、降级、A/B 测试

**边界说明**：

Model 是推理与生成资源层，不直接承载业务流程。

---

## 6. 端到端业务主链路

### [REQ-FLOW-001] 标准任务链路

1. 客户进入 AI 工作台的会话管理
2. 客户用自然语言提出需求
3. 母智能体理解需求并判断是否需要澄清
4. 若需要，继续与客户交互直至需求明确
5. 母智能体创建任务并写入任务中心
6. 系统选择或生成对应 workflow
7. workflow 调度一个或多个子智能体
8. 子智能体调用 skills
9. skills 根据需要调用 tools 与 models
10. 结果回传给母智能体汇总
11. 任务状态写回任务中心
12. 产生的文件写入 Workspace
13. 客户可继续在会话中查看、追问、复用结果

---

## 7. 核心对象关系约束

### [REQ-DATA-001] 对象关系

- 一个会话可对应多个任务
- 一个任务至少关联一个会话
- 一个任务可关联一个或多个 workflow 执行记录
- 一个 workflow 可调用多个 agent / skill / tool / model
- 一个 agent 可绑定多个 skill / tool / model
- 一个任务可产生多个 workspace 文件
- 一个 workspace 文件必须可追溯来源任务或来源会话

---

## 8. AI 文档维护规则

### [REQ-GOV-001] 维护原则

后续 AI 每次进行功能修改时，必须同步维护本文件，至少满足以下要求：

1. 修改前必须先确认本次改动对应的需求编号
2. 修改后必须检查是否影响以下内容：
   - 菜单结构
   - 功能边界
   - 对象关系
   - 状态流转
   - 审核要求
   - 验收标准
3. 若影响以上内容，必须同步修改本文件
4. 若不影响需求基线，也必须在变更记录中说明“无需求变更，仅实现更新”
5. 不允许代码与文档长期脱节

### [REQ-GOV-002] 每次实施后的更新要求

每次完成功能开发后，AI 应追加或更新以下信息：

1. 本次变更关联的需求编号
2. 本次变更涉及的模块
3. 本次变更是否改变需求基线
4. 若改变需求基线，变更原因是什么
5. 本次变更的审核状态

---

## 9. 审核机制

### [REQ-REV-001] 审核目标

确保每次功能修改都符合当前文档定义的需求，不出现“实现偏离需求”或“未审批先变基线”的情况。

### [REQ-REV-002] 审核分类

#### A. 实现一致性审核

用于判断代码/配置/页面实现是否符合当前需求基线：

1. 是否按本文件定义的菜单与模块实施
2. 是否存在超范围开发
3. 是否遗漏必需流程或状态
4. 是否保持对象关系一致
5. 是否保留必要的审核节点

#### B. 需求变更审核

用于判断本次是否修改了需求本身：

1. 是否新增了原文档中不存在的功能
2. 是否修改了模块边界
3. 是否改变了状态流转
4. 是否改变了角色职责
5. 是否改变了数据关系

凡是影响以上内容的修改，必须先更新本文件，再进入“待审核”状态，待人工确认后才可视为正式基线。

### [REQ-REV-003] 审核状态

建议统一使用以下状态：

- 已对齐
- 待审核
- 审核通过
- 审核驳回

### [REQ-REV-004] 每次修改后的最小审核清单

每次功能修改完成后，AI 至少应输出以下审核清单：

- 本次改动关联需求编号
- 是否严格依据当前基线实施
- 是否修改了基线
- 若修改了基线，是否已更新本文档
- 是否存在待人工确认项
- 本次审核状态

---

## 10. AI 实施输出模板

后续 AI 在完成任意功能后，建议按以下模板输出：

```text
【本次变更说明】
1. 关联需求编号：
2. 变更模块：
3. 变更类型：需求变更 / 实现变更 / 文档维护
4. 是否更新本基线文件：是 / 否
5. 若否，原因：

【审核结果】
1. 是否符合当前需求基线：是 / 否
2. 是否新增待审核项：是 / 否
3. 待审核内容：
4. 审核状态：已对齐 / 待审核 / 审核通过 / 审核驳回
```

---

## 11. 变更记录

### [CHANGE-001] 初始基线建立

- 时间：2026-04-21
- 内容：建立 AI 工作台 / Agent 工作台需求基线文档
- 状态：已建立
- 说明：该版本为首版需求基线，用于指导后续 AI 在项目中的落地与持续维护

