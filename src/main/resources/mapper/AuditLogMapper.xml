<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.license.mapper.AuditLogMapper">

    <resultMap id="auditLogMap" type="com.example.license.model.AuditLog">
        <id     property="id"                 column="id"/>
        <result property="tenantId"           column="tenant_id"/>
        <result property="action"             column="action"/>
        <result property="operatorId"         column="operator_id"/>
        <result property="operatorIp"         column="operator_ip"/>
        <result property="oldLicenseDigest"   column="old_license_digest"/>
        <result property="newLicenseDigest"   column="new_license_digest"/>
        <result property="result"             column="result"/>
        <result property="errorMessage"       column="error_message"/>
        <result property="createdAt"          column="created_at"/>
    </resultMap>

    <insert id="insertAuditLog" parameterType="com.example.license.model.AuditLog"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO license_audit_log
            (tenant_id, action, operator_id, operator_ip, old_license_digest,
             new_license_digest, result, error_message, created_at)
        VALUES
            (#{tenantId}, #{action}, #{operatorId}, #{operatorIp}, #{oldLicenseDigest},
             #{newLicenseDigest}, #{result}, #{errorMessage}, #{createdAt})
    </insert>

    <select id="selectByTenantId" parameterType="map" resultMap="auditLogMap">
        SELECT id, tenant_id, action, operator_id, operator_ip, old_license_digest,
               new_license_digest, result, error_message, created_at
        FROM license_audit_log
        WHERE tenant_id = #{tenantId}
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

</mapper>
